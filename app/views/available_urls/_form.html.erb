<%= form_with(model: available_url) do |form| %>
  <% if available_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(available_url.errors.count, "error") %> prohibited this available_url from being saved:</h2>

      <ul>
        <% available_url.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
